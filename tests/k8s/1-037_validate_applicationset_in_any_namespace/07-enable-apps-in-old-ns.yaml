# no appset permissions should be added to target namespaces
apiVersion: argoproj.io/v1beta1
kind: ArgoCD
metadata:
  name: example
  namespace: appset-argocd
spec:
  sourceNamespaces:
    - appset-new-ns
    - appset-old-ns
  applicationSet:
    scmProviders:
      - github.com
