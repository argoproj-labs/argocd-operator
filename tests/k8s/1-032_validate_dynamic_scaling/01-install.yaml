apiVersion: argoproj.io/v1alpha1
kind: ArgoCD
metadata:
  name: argocd
spec:
  controller:
    sharding:
      clustersPerShard: 1
      dynamicScalingEnabled: true
      maxShards: 4
      minShards: 1
